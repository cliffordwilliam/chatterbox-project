[[slides]]
lang = "python"
highlight = [
    2,
]
text = "def generate_presentation(json_path: str, output_path: str):\n    with open(json_path) as f:\n        slides = json.load(f)\n"
voice = "First, we load the JSON file containing our slide data and parse it into a Python dictionary"
duration = 5.48

[[slides]]
lang = "python"
text = "model = ChatterboxTurboTTS.from_pretrained(device=\"cuda\")\nprs = Presentation()\n"
voice = "Next, we initialize the TTS model on CUDA for fast generation, and create a new PowerPoint presentation object"
duration = 6.84

[[slides]]
lang = "python"
text = "for slide_data in slides:\n    slide = prs.slides.add_slide(prs.slide_layouts[1])\n    slide.shapes.title.text = slide_data['title']\n"
voice = "Finally, we loop through each slide in our data, add it to the presentation, and set the title text"
duration = 5.92
